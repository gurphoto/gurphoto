.row-fluid
  .span6
    - $is_hotelier_form = true
    = f.fields_for :hotelier_hotels do |ff|
      .well
        - ff.object.user_id = resource.profile.user_id
        = ff.text_field :user_id

        = f.object.profile.name
      .span6
        - $is_hotelier_form = true
        = link_to 'Выбор гостиницы', hotels_path, class: 'btn btn-large btn-success'
        - ff.object.hotel_id = $hotel_save
      .span3
        = ff.text_field :hotel_id

      .pull-right
        = ff.link_to_remove class: 'btn' do
          %i.icon-minus
          Убрать эту гостиницу из управления
      %br
      %br
= f.link_to_add :hotelier_hotels, class: 'btn' do
  %i.icon-plus
  Добавить ещё одну гостиницу в упраление
